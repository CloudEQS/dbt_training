version: 2

models:
  - name: fact_opportunity_bookings
    description: "Fact table for opportunity bookings at customer-product level"

    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - customer_id
            - product_id
            - opportunity_id